# Dev image: Go server with live reload via air.
FROM golang:1.23-alpine

RUN apk add --no-cache git curl && \
    go install github.com/air-verse/air@v1.52.3

WORKDIR /app

COPY go.mod go.sum* ./
RUN go mod download || true

COPY . .

EXPOSE 8080

CMD ["air", "-c", ".air.toml"]
